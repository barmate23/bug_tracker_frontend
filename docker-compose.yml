services:
  bug-tracker-frontend:
    image: bug-tracker-frontend:latest
    container_name: bug-tracker-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - app_network

networks:
  app_network:
    external: true
    name: updated_orgadmin_rmscadminnetwork
